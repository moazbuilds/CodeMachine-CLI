{
  "name": "codemachine-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild assistant/index.js --bundle --outfile=assistant.js --format=iife",
    "watch": "esbuild assistant/index.js --bundle --outfile=assistant.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
